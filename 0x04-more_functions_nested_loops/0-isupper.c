#include "main.h"

/**
 * is_upper - prototype function.
 *
 * Checks for uppercase character.
 *
 * @c: The character that needs to be checked
 * Return: Always 0.
 */
int _isupper(int c)
{
	for (c = 'A'; c <= 'Z'; c++)
	{
		return (1);
	}
	return (0);
}
